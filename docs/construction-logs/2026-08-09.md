# Kitten Nest v2 construction log — 2026-08-09

## Live state and MCP recovery

The paused free-plan `kitten-nest-mcp` Supabase project was resumed and returned to `ACTIVE_HEALTHY`. The `nest_state/main` row remained present as an object with the expected notebook, coffee-bubble, and weather fields.

The deployed `/api/mcp` endpoint completed its public handshake and anonymous `read_nest_state` call after the project recovered. The already-installed private ChatGPT `猫窝` connection then ran `update_text_target` in `dryRun` mode before publishing a three-line `coffeeCornerBubble` queue. A read-only database check confirmed the new queue, length, and timestamps. No secret was printed, copied into the repository, or requested from Vicky.

## Home-screen and manifest guardrails

The isolated v2 entry now carries the iPhone home-screen metadata already expected by the stable nest: safe-area viewport, standalone capability, translucent status bar, and app title.

Manifest validation now rejects:

- scene navigation to an unknown target;
- text rotation pointed at a non-TextPort object;
- owner-scene objects omitted from their scene card;
- unsupported effect types;
- rendered hotspots, TextPorts, or effects without coordinates;
- coordinates outside normalized base-image bounds.

Coverage initially increased from 54 to 56 checks. `npm run check:v2` and `git diff --check` passed with six scenes, 39 objects, eight controllers, and zero manifest warnings.

## iPhone acceptance corrections

The first complete iPhone sweep found four product mismatches. All were kept on the isolated branch:

- The three beach files still existed in the public Storage bucket, but each 3.4–3.75 MB object could exceed the client timeout. The first same-origin function-proxy attempt also timed out on iPhone. It was removed and replaced with repository-owned 1536 × 1024 WebP delivery variants of about 0.21–0.26 MB; canonical public Storage URLs remain second sources. Coffee corner and the first two beach scenes warm the next static image in sequence.
- The weather advice reused the generic bottom sheet. It now uses the original compact floating-card proportions and a nearly clear backdrop.
- The lower scene docks are again transparent fixed corner hit zones; no arrow glyph, border, blur, or circular control is painted.
- The original transparent upper-left 1.8 second long press now opens a PanelController-owned setup with `房间 / 照片墙 / 其他`. Its exact allowlist is the three historical room overrides `homeOn`, `homeOff`, `gameRoom` plus six `photo0`–`photo5` slots, with image type and 15 MB size checks and no cloud upload. Selection suppression is confined to the armed admin gesture, eliminating the iOS blue selection overlay.
- Lower-corner room navigation now captures the initiating pointer, tolerates 28 px of normal finger drift, and uses larger invisible hit zones without bringing back arrow icons. A final device report exposed CSS Grid auto-placement: hiding the unavailable left dock on Home moved the right dock into column one. Each dock is now pinned to its named column, so hiding its sibling cannot move the live hit zone.

The Storage project remained `ACTIVE_HEALTHY`; its bucket, policies, objects, database, and state were not mutated. The original images were used only as read-only conversion inputs; the static delivery variants add no Vercel function. Coverage is now 72 checks with 41 manifest objects and zero warnings.
