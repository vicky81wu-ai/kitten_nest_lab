# Kitten Nest v2 construction log — 2026-08-09

## Live state and MCP recovery

The paused free-plan `kitten-nest-mcp` Supabase project was resumed and returned to `ACTIVE_HEALTHY`. The `nest_state/main` row remained present as an object with the expected notebook, coffee-bubble, and weather fields.

The deployed `/api/mcp` endpoint completed its public handshake and anonymous `read_nest_state` call after the project recovered. The already-installed private ChatGPT `猫窝` connection then ran `update_text_target` in `dryRun` mode before publishing a three-line `coffeeCornerBubble` queue. A read-only database check confirmed the new queue, length, and timestamps. No secret was printed, copied into the repository, or requested from Vicky.

## Home-screen and manifest guardrails

The isolated v2 entry now carries the iPhone home-screen metadata already expected by the stable nest: safe-area viewport, standalone capability, translucent status bar, and app title.

Manifest validation now rejects:

- scene navigation to an unknown target;
- text rotation pointed at a non-TextPort object;
- owner-scene objects omitted from their scene card;
- unsupported effect types;
- rendered hotspots, TextPorts, or effects without coordinates;
- coordinates outside normalized base-image bounds.

Coverage increased from 54 to 56 checks. `npm run check:v2` and `git diff --check` passed with six scenes, 39 objects, eight controllers, and zero manifest warnings.
