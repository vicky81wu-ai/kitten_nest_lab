# Construction Log - 2026-06-13

This log stores short-lived / historical construction details so permanent docs can stay lean.

Permanent docs should keep only current rules, current IDs, approved coordinates, and stable guardrails.

## Hotspot and coordinate cleanup

- Verified normal `/cloud` after previous hotspot work.
- Removed the old `.photoHot` CSS patch from `api/app-coords.js`.
- Current photo wall hotspot source is `assets/hotspot-positioner.js`.
- Documented future direction: keep JS coordinate cards as runtime truth for now, then consider a static `data/hotspots-runtime.v1.json` after enough hotspots justify it.

## Powder notebook / Hubby note panel

- Fixed duplicate archive behavior.
- Fixed one-save delay: saved text now enters the permanent archive immediately.
- Removed the redundant current-page preview block and top edit/favorite/delete controls.
- Current panel flow is archive-first:

```text
editor -> save -> permanent archive
```

- Key control moved out of the central flow; stored key is replaced through a small paw button.
- Clarified classification: the physical powder notebook is currently a home object; the archive panel is `shared.hubbyNotePanel`, not a coffeeCorner object.

## Naming correction

- Added naming conventions.
- Rule: user-facing names may repeat, but canonical IDs must be scene-scoped.
- Correct current mapping:

```text
home.hubbyNoteHot = home physical powder notebook hotspot
shared.hubbyNotePanel = shared/global archive-first notebook panel
coffeeCorner = no physical powder notebook currently
```

## Admin entry and bottom navigation

- Moved room navigation to bottom edges:

```text
bottom-right = home -> coffeeCorner
bottom-left = coffeeCorner -> home
```

- Replaced visible upper-left `素材` button with a transparent upper-left management zone.
- Current management trigger:

```text
upper-left 28vw x 20vh
long press 1.8s
open existing setup/material panel
```

- `🐾` is the preferred future symbol for settings / admin / management.

## iOS long-press issue and fix

Observed issue:

```text
iOS/WebView showed blue selection handles and copy/query/translate bar during long press.
```

Attempted fix:

```text
-webkit-user-select: none
-webkit-touch-callout: none
preventDefault on long press
```

Regression:

```text
A global touchend guard accidentally blocked all hotspots.
Symptoms: left/right navigation, powder notebook, and admin long press became unreliable or dead.
```

Final fix:

```text
Only touches that start inside the upper-left admin zone may be blocked.
All other hotspot touchend/click behavior must pass through.
```

Vicky verified after the scoped touch guard fix:

```text
left/right room navigation works
home powder notebook works
upper-left long press works
flow feels normal again
```

## Documentation lesson

Do not put full incident diaries into permanent docs.

Use this split:

```text
Permanent docs = current rules / IDs / coordinates / guardrails
Construction logs = incidents / regressions / test paths / day-specific verification notes
```
